# Jarvis Personal – dependencias (Python 3.10+)

# Librería de herramientas para el agente de IA
ai-tools-hub @ git+https://github.com/EL-SICRIO-R3/ia-tools.git

# Dependencias de ai-tools-hub
pyperclip==1.9.0
requests>=2.31.0
beautifulsoup4>=4.12.0
pypdf>=4.0.0
psutil>=5.9.0
opencv-python>=4.9.0
mss>=9.0.0
plyer>=2.1.0

# API de Google Gemini (LLM principal)
google-generativeai==0.8.5

# Nuevo SDK unificado de Google (necesario para generación de imágenes con Gemini API key)
google-genai>=1.0.0

# Carga de variables de entorno desde .env
python-dotenv>=1.0.0

# Reconocimiento de voz – STT  (requiere: brew install portaudio)
SpeechRecognition>=3.10.0
pyaudio>=0.2.14

# Síntesis de voz – TTS neural (Microsoft Edge voices, requiere internet)
edge-tts>=7.0.0
truststore>=0.10.0   # certificados SSL del sistema en macOS/Windows/Linux

# TTS fallback para sistemas no macOS
pyttsx3>=2.90

# Síntesis de voz – TTS ultra-natural (ElevenLabs, requiere ELEVENLABS_API_KEY)
elevenlabs>=1.0.0

# Imágenes – carga, redimensionado y preview en canvas
Pillow>=10.0.0

# Drag-and-drop nativo de archivos en la ventana tkinter
tkinterdnd2>=0.3.0
